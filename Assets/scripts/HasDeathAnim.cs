using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HasDeathAnim : MonoBehaviour
{
    private void Start()
    {
        
    }

    private void Update()
    {
        
    }

    // Start is called before the first frame update
    public void finish()
    {
        Destroy(gameObject);
    }
}
